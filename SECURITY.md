# 安全策略

## 支持的版本

| 版本 | 是否维护 |
|------|----------|
| 0.1.x | ✅ |

项目尚在 0.x 阶段，安全修复只针对最新的次要版本。

## 报告漏洞

gandharva 是一个离线的音频处理库，不涉及网络、认证或密钥，攻击面很小。
但如果你发现了安全相关的问题（例如构造特殊的 WAV / 乐谱输入导致崩溃、
超长循环、内存异常增长等），请：

1. **不要**直接开公开 issue；
2. 通过 GitHub 的 [私密漏洞上报](https://github.com/abenneto/gandharva/security/advisories/new)
   提交，或邮件联系维护者；
3. 尽量附上最小复现输入与环境信息。

我会在合理时间内回复并给出修复计划。

## 使用建议

- 处理不受信任的音频 / 乐谱文件时，注意 `Score` 的时长、音符数没有硬上限——
  超大输入可能占用大量内存与 CPU，请在上层自行限制。
- WAV 解析仅支持 16-bit PCM，其他格式会直接报错，不会尝试「智能」解析。
